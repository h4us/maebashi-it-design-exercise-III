# Next.js + @artcom/react-three-arjs (+ Tailwind CSS) Example

Install dependencies.

```bash
npm i
```

Run with dev mode

```bash
npm run dev
```

Build & export for Static Site

```bash
npm run build && npm run export 
```